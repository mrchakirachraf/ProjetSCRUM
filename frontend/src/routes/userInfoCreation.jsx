import React, { useState } from "react";

const UserInfoCreation = () => {
  
  return (
    <div className="bg-white min-h-screen flex items-center justify-center">
      user info CreationPage
    </div>
  );
};

export default UserInfoCreation;
